# game-engine: Demo game engine concept

IDK. I wanna make a roguelike from scratch

## Installation

To build from current directory (`/game-engine`):

```bash
cmake -B build && cmake --build build
```

To run from current directory (`/game-engine`):

```bash
build/bin/main
```
